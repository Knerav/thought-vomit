<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class = "container">
    <%= link_to "ThoughtVomit", root_path, class: "navbar-brand", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link active" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to 'View articles', articles_path, class: "dropdown-item" %></li>
            <% if logged_in? %>
              <li><%= link_to 'Create article', new_article_path, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link active" %>
        </li>
      </ul>
      <class="container">
        <% if logged_in? %>
          <li class="nav-item dropdown btn">
            <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to 'View your profile', user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to 'Edit your profile', edit_user_path(current_user), class: "dropdown-item" %></li>
            </ul>
          </li>
          <%= link_to 'Logout', logout_path, class: "btn btn-secondary", method: :delete %>
        <% else %>
          <%= link_to 'Login', login_path, class: "btn btn-primary ms-1" %>
          <%= link_to 'Signup', signup_path, class: "btn btn-primary ms-1" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>