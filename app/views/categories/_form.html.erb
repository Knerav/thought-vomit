<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New User</title>
  </head>
  <body>
    <div class = "container">
      <div class = "row justify-content-center">
        <%= render 'shared/errors', obj: @category %>
        <div class="card p-4 mt-5 shadow mb-5 col-8">
          <%= form_with(model: @category, local: true) do |f| %>
            
            <div class="mb-4">
              <%= f.label :name, "Category name", class: "form-label" %>
              <%= f.text_field :name, class: "form-control", id: "category" %>
              <div id="categoryHelp" class="form-text">Enter a unique category that is 3 characters minimum & 25 maximum.</div>
            </div>
            
            <%= link_to 'Cancel', @category.new_record? ? categories_path : category_path(@category), class: "btn btn-outline-secondary" %>
            <%= f.submit class: "btn btn-success" %>
          <% end %>
        </div>
      </div>
    </div>
  </body>
</html>