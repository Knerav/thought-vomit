<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  </head>
  <body>
    <div class = "row justify-content-center">
      <%= render 'shared/errors', obj: @article %>
      <div class="card p-4 mt-5 shadow mb-5 col-8">
        <%= form_with(model: @article, local: true) do |f| %>
          
          <div class="mb-4">
            <%= f.label :title, class: "form-label" %>
            <%= f.text_field :title, class: "form-control", id: "title" %>
            <div id="titleHelp" class="form-text">The title has to be 6 characters minimum & 100 maximum.</div>
          </div>
          
          <div class="mb-4">
            <%= f.label :description, class: "form-label" %>
            <%= f.text_area :description, rows: 8, class: "form-control", id: "description" %>
            <div id="descriptionHelp" class="form-text">The description has to be 10 characters minimum & 300 maximum.</div>
          </div>
          
          <div class="mb-4">
            <%= f.label :category, class: "form-label" %>
            <%= f.collection_select(:category_ids, Category.all, :id, :name, { prompt: "Please enter the appropriate categories" }, { multiple: true, class: "form-select" }) %>
          </div>

          <%= link_to 'Cancel', articles_path, class: "btn btn-outline-secondary" %>
          <%= f.submit class: "btn btn-success" %>
        <% end %>
      </div>
    </div>
  </body>
</html>